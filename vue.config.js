// vue.config.js
module.exports = {
  // options...
};
var config = {
  mimeTypes: {
    _comment:
      "This section maps MIME types to file extensions. It is used by libnhttpd to determine the MIME type for a file that is to be served from its file serving URI.",
    "application/json": ["json"]
  }
};
